/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        loader: "default", // Default image loader
        path: "/_next/image", // Default image path
    },
};

export default nextConfig;